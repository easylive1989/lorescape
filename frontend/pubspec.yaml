name: context_app
description: "Lorescape — your AI pocket historian"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 0.0.7+7

environment:
  sdk: ^3.8.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  
  # State management with Riverpod
  flutter_riverpod: ^2.6.1
  
  # Supabase for authentication and backend
  supabase_flutter: ^2.8.2
  
  # Google Sign In
  google_sign_in: ^6.2.1
  
  # Location services
  geolocator: ^14.0.2

  # Map (探索頁全螢幕地圖，見 docs/adr/0005-map-tile-provider.md)
  flutter_map: ^8.1.1
  # 釘死確切版本：9.x 仍是 prerelease，beta 之間可能有破壞性變更，不用 caret
  # range。維護者建議正式 app 用 8.0.0 或 9.0.0-beta.x；8.0.0 只支援
  # flutter_map 7.x，故取 9.0.0-beta.9（相依 flutter_map ^8.1.1）。
  # 10.0.0-beta 相依 flutter_gpu + Flutter dev channel，不可用。
  vector_map_tiles: 9.0.0-beta.9
  # ThemeReader：把本地重新上色的 style JSON 轉成可渲染的 theme
  # （vector_map_tiles 的傳遞依賴，但我們直接引用，故明寫）
  vector_tile_renderer: ^6.1.0
  # flutter_map 的座標型別（LatLng），直接被我們的程式碼引用，故明寫依賴
  latlong2: ^0.9.1

  # HTTP requests
  http: ^1.2.2
  cached_network_image: ^3.4.1
  intl: ^0.20.2
  go_router: ^17.0.0
  palette_generator: ^0.3.3+7
  flutter_quill: ^11.5.0
  easy_localization: ^3.0.7
  photo_view: ^0.15.0
  path_provider: ^2.1.5
  url_launcher: ^6.3.1
  flutter_tts: ^4.2.3
  uuid: ^4.5.2
  package_info_plus: ^8.3.1
  just_audio: ^0.10.5
  scroll_to_index: ^3.0.1
  sign_in_with_apple: ^7.0.1
  crypto: ^3.0.7
  # Pinned: firebase_core 4.11.0 pins Firebase iOS SDK 12.15.0, which raises the
  # FirebaseAnalytics minimum to iOS 16 and breaks pod install for our iOS 15
  # deployment target. 4.9.0 keeps Firebase SDK 12.13.0 (iOS 15). See the
  # firebase_app_check pin below; revisit together if iOS min is raised to 16.
  firebase_core: 4.9.0
  # Pinned to match native firebase_core 4.9.0's Pigeon protocol (14 fields,
  # no recaptchaSiteKey). 7.1.0 decodes a 15th field that native 4.9.0 never
  # sends, crashing Firebase.initializeApp with RangeError. Bump together with
  # firebase_core only.
  firebase_core_platform_interface: 7.0.1
  firebase_ai: ^3.6.1
  
  # Local caching for API optimization
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_cache_manager: ^3.4.1
  
  # Lazy loading for images
  visibility_detector: ^0.4.0+2
  equatable: ^2.0.7
  shared_preferences: ^2.3.4
  logging: ^1.3.0
  purchases_flutter: ^9.13.2

  # Receive shared content from other apps (e.g. Google Maps)
  receive_sharing_intent: ^1.8.1

  # Share content to other apps (social media, messaging, etc.)
  share_plus: ^10.1.4
  webview_flutter: ^4.13.1
  pdf: ^3.12.0
  printing: ^5.14.3

  # Onboarding
  introduction_screen: ^3.1.14
  firebase_analytics: ^12.4.1
  google_fonts: ^8.1.0
  # Pinned to the release compatible with firebase_core 4.9.0 (iOS 15). 0.4.5
  # requires firebase_core ^4.11.0 (Firebase SDK 12.15.0 / iOS 16). Same
  # class-based provider API (providerAndroid/providerApple) used in main.dart.
  firebase_app_check: 0.4.4+1
  marionette_flutter: ^0.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.3

  # E2E testing with Patrol
  patrol: ^3.20.0
  mocktail: ^1.0.4
  flutter_native_splash: ^2.4.7

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/categories/
    - assets/images/onboarding/
    - assets/images/subscription/
    - assets/images/splash_mark.png
    - assets/translations/
    # 由 tool/build_map_style.py 產生，勿手改（見 docs/adr/0005-map-tile-provider.md）
    - assets/map/
    # 由 tool/build_world_outline.py 從 Natural Earth 110m 產生，勿手改
    - assets/geo/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon/launch_icon_light.png"
  min_sdk_android: 21

flutter_native_splash:
  color: "#F7F1E6"
  image: assets/images/splash_mark.png
  color_dark: "#F7F1E6"
  image_dark: assets/images/splash_mark.png
  android_12:
    color: "#F7F1E6"
    image: assets/images/splash_mark.png
    color_dark: "#F7F1E6"
    image_dark: assets/images/splash_mark.png
  android: true
  ios: true
  web: false

